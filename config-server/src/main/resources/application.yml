server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          #配置github
          uri: https://github.com/1836492301/zzy-4.18-homeWork
          #配置搜索目录
          search-paths:
            - configfiles
      #配置分支
      label: master